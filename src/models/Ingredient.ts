export interface Ingredient {
    name: string
    quantity: number
    id: number
    recipe_id: number
}

export interface IngredientDTO {
    name: string
    quantity: number
    recipe_id: number
  }